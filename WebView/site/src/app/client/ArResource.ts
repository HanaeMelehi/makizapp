export interface ArResource{

  id: string;
  name: string;
  ARjsMarker: string; // Assuming ARjsMarker is also a string, update as needed
  thumbnail: string; // Assuming ImageAsset is also a string, update as needed
  imageAsset: string; // Assuming ImageAsset is also a string, update as needed
  videoAsset: string; // Assuming VideoAsset is also a string, update as needed
  soundAsset: string; // Assuming SoundAsset is also a string, update as needed
}
